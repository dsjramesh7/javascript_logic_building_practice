// print "you can drive" or "you can't drive" based on age greater than 18 using ternary operator

function canDrive(age) {
  console.log(age >= 18 ? "you can drive" : "you can't drive");
}

canDrive(4);
canDrive(14);
canDrive(18);
canDrive(21);
